# Local development
DATABASE_URL=postgresql://yourusername:yourpassword@localhost:5432/rubric_db
OPENAI_API_KEY=sk-...
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=15
MAX_PAGES=23

# Auth (shared secret must match frontend NEXTAUTH_SECRET exactly)
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
ADMIN_EMAIL=your@email.com

# Production (set these via `heroku config:set`, not in this file)
# DATABASE_URL=postgresql://user:pass@ep-xxx.pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
# OPENAI_API_KEY=sk-...
# FRONTEND_URL=https://scorely-ai.vercel.app   <-- REQUIRED in production (used for CORS)
# UPLOAD_DIR=/tmp/uploads
# NEXTAUTH_SECRET=...
# ADMIN_EMAIL=...
